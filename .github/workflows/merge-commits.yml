name: Merge commits to next branch

on:
  push:
    branch: [ test_main ]

env:
  TARGET_BRANCH: next
  SOURCE_BRANCH: test_main

jobs:
  dequeue:
    runs-on: ubuntu-latest
    if: ${{ github.repository_owner }} == 'microsoft'
    steps:
      - run: npm install @azure/storage-queue
      - run:
          echo ${{ github.repository_owner }}
          echo ${{ github.repositoryUrl }}
      - uses: actions/github-script@v6
        if: ${{ github.repository_owner }} == 'microsoft'
        id: deque-message
        with:
          script: |
            const { QueueClient, QueueServiceClient } = require('@azure/storage-queue')
            const connectionString = `${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}`;
            const queueName = `${{ secrets.AZURE_QUEUE_NAME }}`;
            const queueServiceClient = QueueServiceClient.fromConnectionString(connectionString);
            const queueClient = queueServiceClient.getQueueClient(queueName);
            var receivedMessages = await queueClient.peekMessages();
            const message = receivedMessages.peekedMessageItems[0].messageText;
            const firstMessage = JSON.parse(message);
            if(firstMessage === undefined) { return 0; }
            console.log("message----", firstMessage);
            console.log("sha---", firstMessage.sha);
            console.log("label---", firstMessage.label);
            console.log("author---", firstMessage.author);
            return firstMessage;
    outputs:
      LABEL: ${{ fromJSON(steps.deque-message.outputs.result).label }}
      AUTHOR: ${{ fromJSON(steps.deque-message.outputs.result).author }}
      SHA: ${{ fromJSON(steps.deque-message.outputs.result).sha }}
  create-branch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    needs: [ dequeue ]
    if: (needs.dequeue.result) == 'success'
    steps:
      - run:
          echo ${{needs.dequeue.outputs.LABEL}}
      - name: Git checkout
        if: ${{needs.dequeue.outputs.LABEL}} == 'queued'
        uses: actions/checkout@v2
        with:
          token: ${{ secrets.WORKFLOW_TOKEN }}
          fetch-depth: '0'
      - name: Create branch
        if: ${{needs.dequeue.outputs.LABEL}} == 'queued'
        run: |
          git config user.name "Automation Bot"
          git config user.email "<>"
          git fetch origin
          git checkout $SOURCE_BRANCH
          git checkout -b main-next-${{needs.dequeue.outputs.SHA}}
          git fetch --all
          git reset --hard ${{needs.dequeue.outputs.SHA}}
          git push --set-upstream origin main-next-${{needs.dequeue.outputs.SHA}}
  pull-request:
    runs-on: ubuntu-latest
    concurrency:
      group: group-pr
      cancel-in-progress: false
    needs: [ create-branch, dequeue ]
    if: needs.create-branch.result == 'success'
    steps:
      - run: npm install @octokit/core
      - name: Create PR using octokit
        uses: actions/github-script@v6
        with:
          token: ${{ secrets.WORKFLOW_TOKEN }}
          script: | # add reviwers to PR
            const { Octokit } = require("@octokit/core");
            const token = `${{ secrets.GITHUB_TOKEN }}`;
            const sha = 'main-next-${{needs.dequeue.outputs.SHA}}';
            const author = '${{needs.dequeue.outputs.AUTHOR}}';
            const octokit = new Octokit({ auth: token })
            const newPr = await octokit.request('POST /repos/{owner}/{repo}/pulls', {
              owner: 'sonalivdeshpande',
              repo: 'FluidFramework',
              title: 'Main Next Integrate',
              body: 'This commit is queued for getting merged with the next branch. Please make sure to resolve any conflicts/CI failures seen on this PR. Thank you!',
              head: sha,
              base: 'next'
            });
            await octokit.request('POST /repos/{owner}/{repo}/issues/{issue_number}/assignees', {
              owner: 'sonalivdeshpande',
              repo: 'FluidFramework',
              issue_number: newPr.data.number,
              assignees: [ author ]
            });
  # // await till PR status changes to approved
  # const prReview = async () => {
  #   await octokit.request('GET /repos/{owner}/{repo}/pulls/{pull_number}/reviews', {
  #     owner: 'sonalivdeshpande',
  #     repo: 'FluidFramework',
  #     pull_number: newPr.data.number,
  #   });
  # }
  # if(prReview.status !== 'APPROVED') { prReview.status = setInterval(() => { prReview() }, 20000); }
  remove-from-queue:
    runs-on: ubuntu-latest
    concurrency:
      group: group-pr
      cancel-in-progress: false
    needs: [ pull-request, dequeue ]
    if: ( needs.pull-request.result == 'success' )
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 14
      - run: npm ci
      - run: npm install @azure/storage-queue
      - uses: actions/github-script@v6
        with:
          script: |
            const { QueueClient, QueueServiceClient } = require('@azure/storage-queue')
            const connectionString = `${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}`;
            const queueServiceClient = QueueServiceClient.fromConnectionString(connectionString);
            const queueName = `${{ secrets.AZURE_QUEUE_NAME }}`;
            const queueClient = queueServiceClient.getQueueClient(queueName);
            receivedMessages = await queueClient.receiveMessages();
            var message = receivedMessages.receivedMessageItems[0];
            console.log("Deleting message: ", message.messageText);
            try {
              await queueClient.deleteMessage(message.messageId, message.popReceipt);
            } catch (err) {
              console.log("ERR!!--", error);
            }
